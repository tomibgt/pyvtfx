[project]
name = "pyvtfx"
version = "0.1.0"
description = "A Python package for ANSI controls on terminals"
authors = [
  { name = "Tomi \"bgt\" Suovuo", email = "bgt@sci.fi" }
]
requires-python = ">=3.7"

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"
